# Saved Messages Feature - Visual Diagram

## Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         User Interface                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    Chat Message                          │  │
│  │  ┌───────────────────────────────────────────────┐      │  │
│  │  │ AI: Here's how to implement the feature...    │      │  │
│  │  │                                                │      │  │
│  │  │ 1. Create a new component                     │      │  │
│  │  │ 2. Add the necessary props                    │      │  │
│  │  │ 3. Implement the logic                        │      │  │
│  │  └───────────────────────────────────────────────┘      │  │
│  │                                                          │  │
│  │  Action Buttons:                                        │  │
│  │  [✏️ Edit] [🔄 Retry] [🗑️ Delete] [📌 Pin]             │  │
│  │  [📤 Save] [↩️ Insert] [🌐 Translate] [📋 Copy]         │  │
│  │          ↓         ↓                                     │  │
│  └──────────┼─────────┼─────────────────────────────────────┘  │
│             │         │                                         │
│    onClick  │         │  onClick                                │
│  onSaveMsg  │         │  onInsertMsg                            │
└─────────────┼─────────┼─────────────────────────────────────────┘
              │         │
              ↓         ↓
┌─────────────────────────────────────────────────────────────────┐
│                     Frontend Handlers                           │
│                    (chat.tsx)                                   │
│                                                                 │
│  onSaveMessage(msg)              onInsertMessage(msg)          │
│       ↓                                  ↓                      │
│       │                          setUserInput(msg.content)     │
│       │                          inputRef.focus()              │
│       ↓                                                         │
│  fetch('/api/save-message')                                    │
│       ↓                                                         │
└───────┼─────────────────────────────────────────────────────────┘
        │
        ↓
┌─────────────────────────────────────────────────────────────────┐
│                    Backend API Layer                            │
│              /api/save-message/route.ts                         │
│                                                                 │
│  POST /api/save-message                                        │
│       ↓                                                         │
│   1. Validate input (content, role)                           │
│   2. Generate unique ID (nanoid)                              │
│   3. Create SavedMessage object                               │
│       ↓                                                         │
└───────┼─────────────────────────────────────────────────────────┘
        │
        ├─────────────┬──────────────┐
        ↓             ↓              ↓
┌──────────────┐ ┌─────────────┐ ┌──────────────┐
│   File       │ │   Redis     │ │  Response    │
│   Storage    │ │   Storage   │ │  to Client   │
│              │ │  (optional) │ │              │
│ data/        │ │             │ │ {success,    │
│ saved_       │ │ saved_      │ │  message}    │
│ messages.    │ │ message:id  │ │              │
│ json         │ │             │ │              │
└──────────────┘ └─────────────┘ └──────┬───────┘
                                        │
                                        ↓
┌─────────────────────────────────────────────────────────────────┐
│                     Frontend Store Layer                        │
│              app/store/saved-messages.ts                        │
│                                                                 │
│  useSavedMessageStore.add(message)                             │
│       ↓                                                         │
│  Store in Zustand state + localStorage                         │
│       ↓                                                         │
│  Add to Fuse.js search index                                   │
└─────────────────────────────────────────────────────────────────┘
```

## Prompt Integration Flow

```
User Types: "/saved"
       ↓
┌─────────────────────────────────────────────────────────────────┐
│                    Input Handler                                │
│                  (onInput function)                             │
│                                                                 │
│  text.startsWith("/saved")                                     │
│       ↓                                                         │
│  searchText = text.slice(6).trim()                             │
│       ↓                                                         │
│  savedMessages = savedMessageStore.search(searchText)          │
└───────┼─────────────────────────────────────────────────────────┘
        │
        ↓
┌─────────────────────────────────────────────────────────────────┐
│                   Fuse.js Search Engine                         │
│         SavedMessageSearchService                               │
│                                                                 │
│  Fuzzy match on message.content                                │
│       ↓                                                         │
│  Return matching saved messages                                │
└───────┼─────────────────────────────────────────────────────────┘
        │
        ↓
┌─────────────────────────────────────────────────────────────────┐
│                Transform to RenderPrompt[]                      │
│                                                                 │
│  savedMessages.map(msg => ({                                   │
│    title: `💾 ${msg.content.substring(0, 30)}...`,            │
│    content: msg.content,                                       │
│    id: msg.id                                                  │
│  }))                                                            │
└───────┼─────────────────────────────────────────────────────────┘
        │
        ↓
┌─────────────────────────────────────────────────────────────────┐
│                    setPromptHints(hints)                        │
│                                                                 │
│  Triggers <PromptHints> component to render                   │
└───────┼─────────────────────────────────────────────────────────┘
        │
        ↓
┌─────────────────────────────────────────────────────────────────┐
│                    UI: Prompt Dropdown                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  💾 Meeting notes from yesterday...                     │  │
│  │  [Full content: Here are the meeting notes...]          │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │  💾 Code example for React hooks...                     │  │
│  │  [Full content: Here's a complete example...]           │  │
│  └──────────────────────────────────────────────────────────┘  │
│                      ↓ User selects                             │
└───────┼─────────────────────────────────────────────────────────┘
        │
        ↓
┌─────────────────────────────────────────────────────────────────┐
│              onPromptSelect(prompt)                             │
│                                                                 │
│  setUserInput(prompt.content)                                  │
│       ↓                                                         │
│  Message content fills input                                   │
│       ↓                                                         │
│  Ready to edit/send                                            │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow Diagram

```
┌─────────────────┐
│   User Action   │
│  (Click Save)   │
└────────┬────────┘
         │
         ↓
┌────────────────────────────────────────────────────────┐
│              onSaveMessage Handler                     │
│  1. Extract message content                           │
│  2. Call API                                          │
│  3. Update local store                                │
│  4. Show toast                                        │
└────────┬───────────────────────────────────────────────┘
         │
         ├──────────────────┬─────────────────────┐
         ↓                  ↓                     ↓
┌──────────────────┐ ┌─────────────────┐ ┌──────────────┐
│  API Endpoint    │ │  Local Store    │ │  UI Toast    │
│  POST /api/      │ │  Zustand +      │ │  Success     │
│  save-message    │ │  localStorage   │ │  Message     │
└────────┬─────────┘ └─────────┬───────┘ └──────────────┘
         │                     │
         ├─────────┬───────────┤
         ↓         ↓           ↓
    ┌────────┐ ┌─────┐  ┌──────────┐
    │  File  │ │Redis│  │ Memory   │
    │ .json  │ │ KV  │  │ State    │
    └────────┘ └─────┘  └──────────┘
                              │
         ┌────────────────────┘
         │
         ↓
┌──────────────────┐
│  Fuse.js Index   │
│  Updated         │
└──────────────────┘
         │
         ↓
┌──────────────────┐
│  Available for   │
│  /saved search   │
└──────────────────┘
```

## State Management

```
┌─────────────────────────────────────────────────────────────┐
│         useSavedMessageStore (Zustand)                      │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  State:                                               │  │
│  │  {                                                    │  │
│  │    messages: {                                        │  │
│  │      "msg-id-1": {                                    │  │
│  │        id: "msg-id-1",                                │  │
│  │        content: "Message text...",                    │  │
│  │        role: "user",                                  │  │
│  │        savedAt: 1234567890,                           │  │
│  │        sessionId: "session-123"                       │  │
│  │      },                                               │  │
│  │      "msg-id-2": { ... }                              │  │
│  │    }                                                  │  │
│  │  }                                                    │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                             │
│  Methods:                                                   │
│  • add(message) ──────────→ Add new saved message         │
│  • get(id) ───────────────→ Retrieve by ID                │
│  • remove(id) ────────────→ Delete saved message          │
│  • getAll() ──────────────→ Get all (sorted by date)      │
│  • search(text) ──────────→ Fuzzy search messages         │
│                                                             │
│  Persistence:                                               │
│  • localStorage key: "saved-message-store"                 │
│  • Auto-save on state change                               │
│  • Auto-restore on app load                                │
└─────────────────────────────────────────────────────────────┘
```

## Component Hierarchy

```
<Chat>
  │
  ├─ State: savedMessageStore = useSavedMessageStore()
  │
  ├─ Handlers:
  │    ├─ onSaveMessage(message)
  │    ├─ onInsertMessage(message)
  │    └─ onInput(text) ──→ Handle "/saved" trigger
  │
  ├─ <PromptHints prompts={promptHints} />
  │    │
  │    └─ Displays saved messages when "/saved" typed
  │
  └─ <ChatMessage>
       │
       └─ <ChatActions>
            ├─ <ChatAction icon={UploadIcon} text="Save" />
            └─ <ChatAction icon={ReturnIcon} text="Insert" />
```

## Search Flow Detail

```
Input: "/saved meeting"
   │
   ↓
1. Detect trigger: text.startsWith("/saved")
   │
   ↓
2. Extract search term: "meeting"
   │
   ↓
3. Call: savedMessageStore.search("meeting")
   │
   ↓
4. Fuse.js searches message.content field
   │
   ↓
5. Returns matches sorted by relevance:
   [
     { content: "Meeting notes...", score: 0.1 },
     { content: "Next meeting agenda...", score: 0.3 }
   ]
   │
   ↓
6. Transform to RenderPrompt format
   │
   ↓
7. setPromptHints(prompts)
   │
   ↓
8. UI displays dropdown with matches
```

## Error Handling

```
onSaveMessage
   │
   ├─ try {
   │    ├─ fetch API ──→ Success ──→ Update store ──→ Show toast
   │    └─ fetch API ──→ Error ───→ catch block
   │  }
   │
   └─ catch (error) {
        ├─ Log error to console
        └─ Show error toast: "Failed to save message"
      }

API Endpoint
   │
   ├─ try {
   │    ├─ Validate input ──→ Invalid ──→ Return 400
   │    ├─ Save to file ────→ Success
   │    ├─ Save to Redis ───→ Success (optional)
   │    │                  └→ Error ──→ Log but continue
   │    └─ Return 200 with message
   │  }
   │
   └─ catch (error) {
        ├─ Log error
        └─ Return 500
      }
```
